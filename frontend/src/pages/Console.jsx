import LiveKitModal from "../components/LiveKitModal";

const Console = () => {
  return (
    <div className="console-page">
      <LiveKitModal setShowSupport={() => {}} />
    </div>
  );
};

export default Console;
